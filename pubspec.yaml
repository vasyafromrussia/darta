name: darta
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.8.0 <3.0.0"

dependencies:
  build: ^1.3.0
  source_gen: ^0.9.5

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
  build_runner: ^1.10.0
